inherits: base_desktop
profile_id: barsim
ui:
  brand_id: base
  window_defaults: { width: 1280, height: 800, maximized: false }
capabilities:
  enable: [cap.jobs, cap.logs.panel, cap.command_palette]
plugins:
  include: [panel_jobs, panel_logs]