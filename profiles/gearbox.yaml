inherits: base_desktop
profile_id: gearbox
ui:
  brand_id: base
  window_defaults: { width: 1600, height: 900, maximized: false }
capabilities:
  enable: [cap.jobs, cap.command_palette]
plugins:
  include: [panel_jobs, panel_logs]